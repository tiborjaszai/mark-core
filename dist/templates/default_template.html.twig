<!DOCTYPE html>
<html lang="{{ context.config.language }}">
    <head>
        <meta charset="UTF-8">
        {% block meta %}{% endblock %}

        <title>{{ node.title ?? context.config.title }}</title>

        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        {{ node.content }}

        {% block javascripts %}{% endblock %}
    </body>
</html>