<!DOCTYPE html>
<html lang="{{ context.language }}">
    <head>
        <meta charset="UTF-8">
        {% block meta %}{% endblock %}

        <title>{{ node.title ?? context.title }}</title>

        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        {{ content }}

        {% block javascripts %}{% endblock %}
    </body>
</html>