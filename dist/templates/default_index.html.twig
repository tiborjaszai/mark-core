<!DOCTYPE html>
<html lang="{{ lang ?? 'en' }}">
    <head>
        <meta charset="UTF-8">
        {% block meta %}{% endblock %}

        <title>{{ title ?? 'Mark - static site generator' }}</title>

        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        {{ content }}

        {% block javascripts %}{% endblock %}
    </body>
</html>